spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: article
      fail-fast: true
      username: user
      password: ${CONFIG_SERVICE_PASSWORD}
  application:
    name: article